package assignment3;

public enum DANGLING_COUNTER {
	
	/*
	 * These are the counters used in the MapReduce program to keep track of the number of unique
	 * page nodes being processed (COUNT) and the summation of the normalized page ranks of 
	 * dangling page nodes (DELTA)
	 */

	DELTA,
	COUNT
	
}
